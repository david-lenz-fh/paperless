info:
  name: addDocument
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/documents"
  body:
    type: json
    data: |-
      {
        "title": "document 1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
